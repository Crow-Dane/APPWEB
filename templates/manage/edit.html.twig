{# templates/manage/edit.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Modifier l'étudiant {{ ancien_etudiant.nom }}{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-2">
                {# Sidebar inclusion, if necessary #}
                {# {{ include('sidebar.html.twig') }} #}
            </div>
            <div class="col-md-10">
                <h1 class="text-center my-4 mb-4">Modifier l'étudiant {{ ancien_etudiant.nom }}</h1>

                <div class="card p-4 mb-4">
                    {{ include('manage/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                    {{ include('manage/_delete_form.html.twig') }}

                </div>

            </div>
        </div>
    </div>
{% endblock %}
