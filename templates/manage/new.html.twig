{% extends 'base.html.twig' %}

{% block title %}Nouvel Ancien Étudiant{% endblock %}

{% block body %}
<div class="center">
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-2">
                {# Sidebar inclusion, if necessary #}
                {# {{ include('sidebar.html.twig') }} #}
            </div>
            <div class="col-md-10">
            <h1 class="text-center my-4 mb-4">Créer un nouvel Ancien Étudiant</h1>
                
                <div class="card p-4">
                    {{ include('manage/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
